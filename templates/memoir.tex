% memoria.tex
% Plantilla Pandoc para memorias académicas.
% Clase: book (LuaLaTeX). Compatible con default.latex y memoria.yaml.
% Copyright © 2026 Carlos Llamedo (MIT Licence)
% ────────────────────────────────────────────────────────────────────────────

% ── Estándar del PDF ─────────────────────────────────────────────────────────

% document.metadata.latex

$if(pdfstandard)$
\DocumentMetadata{
$if(pdfstandard.version)$
  pdfversion=$pdfstandard.version$,
$endif$
$if(pdfstandard.standards)$
  pdfstandard={$for(pdfstandard.standards)$$it$$sep$,$endfor$},
$endif$
$if(pdfstandard.tagging)$
  tagging=on,
$endif$
$if(lang)$
  lang=$lang$,
$endif$
  xmp=true}
$endif$

% ── Opciones para paquetes ───────────────────────────────────────────────────

% passoptions.latex

\PassOptionsToPackage{unicode$for(hyperrefoptions)$,$hyperrefoptions$$endfor$}{hyperref}
\PassOptionsToPackage{hyphens}{url}
$if(colorlinks)$
\PassOptionsToPackage{dvipsnames,svgnames,x11names}{xcolor}
$endif$

\documentclass[
$if(fontsize)$
  $fontsize$,
$else$
  12pt,
$endif$
$if(papersize)$
  $papersize$paper,
$else$
  a4paper,
$endif$
$if(classoption)$
  $for(classoption)$$classoption$$sep$, $endfor$
$else$
  twoside,
  openany
$endif$
]{book}

% ── Colores ──────────────────────────────────────────────────────────────────

\usepackage{xcolor}
\definecolor{linkblue}{RGB}{0, 70, 150}
\definecolor{purpleblue}{RGB}{72, 61, 139}

% ── Geometría ────────────────────────────────────────────────────────────────

$if(geometry)$
\usepackage[$for(geometry)$$geometry$$sep$,$endfor$]{geometry}
$else$
\usepackage[
  top        = 3cm,
  bottom     = 3cm,
  left       = 3cm,
  right      = 3cm,
  headheight = 14.5pt,
  % la mitad del margen superior menos la altura del encabezado
  headsep    = \dimexpr1.5cm - 14.5pt\relax,
  % la mitad del margen inferior
  footskip   = 1.5cm,
]{geometry}
$endif$

% ── Matemáticas ──────────────────────────────────────────────────────────────

% fonts.latex

\usepackage{unicode-math}
\defaultfontfeatures{Scale=MatchLowercase}
\defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}

$if(cancel)$
\usepackage{cancel}
$endif$

% ── Numeración de secciones ──────────────────────────────────────────────────

$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$5$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen}
$endif$

% ── Fuentes ──────────────────────────────────────────────────────────────────

% font-settings.latex

\usepackage{luaotfload}

$if(mainfont)$
\setmainfont[$for(mainfontoptions)$$mainfontoptions$$sep$,$endfor$]{$mainfont$}
$else$
\directlua{luaotfload.add_fallback("mainfontfallback",{"Source Serif 4:mode=harf"})}
\setmainfont[
  RawFeature={fallback=mainfontfallback}$for(mainfontoptions)$,$mainfontoptions$$endfor$
]{Arno Pro}
$endif$

$if(sansfont)$
\setsansfont[$for(sansfontoptions)$$sansfontoptions$$sep$,$endfor$]{$sansfont$}
$else$
\directlua{luaotfload.add_fallback("sansfontfallback",{"Source Sans 3:mode=harf"})}
\setsansfont[
  RawFeature={fallback=sansfontfallback}
]{Myriad Pro}
$endif$

$if(monofont)$
\setmonofont[$for(monofontoptions)$$monofontoptions$$sep$,$endfor$]{$monofont$}
$else$
\setmonofont{Source Code Pro}
$endif$

$if(mathfont)$
\setmathfont[$for(mathfontoptions)$$mathfontoptions$$sep$,$endfor$]{$mathfont$}
$else$
\setmathfont{IBM Plex Math}
$endif$

\usepackage[$if(microtypeoptions)$$for(microtypeoptions)$$microtypeoptions$$sep$,$endfor$$else$protrusion=true$endif$]{microtype}
\UseMicrotypeSet[protrusion]{basicmath}

% Familia de display
$if(titlesans)$
\newcommand{\metafont}{\sffamily}
\newcommand{\metacaps}[1]{\MakeUppercase{#1}}
\newcommand{\capsfont}{\sffamily\upshape}
$elseif(titlefont)$
\newfontfamily\displayfamily[Numbers=OldStyle, $for(titlefontoptions)$$titlefontoptions$$sep$,$endfor$]{$titlefont$}
\newcommand{\metafont}{\displayfamily}
\newcommand{\metacaps}[1]{\textsc{\MakeLowercase{#1}}}
\newcommand{\capsfont}{\displayfamily\scshape}
$else$
\newcommand{\metafont}{\normalfont}
\newcommand{\metacaps}[1]{\textsc{\MakeLowercase{#1}}}
\newcommand{\capsfont}{\scshape}
$endif$

$if(bodysans)$
\renewcommand{\familydefault}{\sfdefault}
$if(titlefont)$$else$
\renewcommand{\metacaps}[1]{\MakeUppercase{#1}}
\renewcommand{\capsfont}{\sffamily\upshape}
$endif$
$endif$

% \textsmall: tamaño reducido para notas, citas, captions y código.
\newcommand{\textsmall}{$if(smallsize)$$smallsize$$else$\normalsize$endif$}

% ── Microtipografía ──────────────────────────────────────────────────────────

$if(strikeout)$
\usepackage{luacolor}
\usepackage[soul]{lua-ul}
$endif$

% ── Interlineado y párrafo ────────────────────────────────────────────────────

\usepackage{setspace}
\setstretch{$if(linestretch)$$linestretch$$else$1.5$endif$}

\newlength{\ParIndent}
% Otras posibilidades: 0.5in, 1cm, 2em
\setlength{\ParIndent}{$if(parindent)$$parindent$$else$1cm$endif$}

$if(noindent)$
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.25\baselineskip plus 2pt minus 1pt}
$else$
\setlength{\parindent}{\ParIndent}
\setlength{\parskip}{0pt}
$endif$

% Control de viudas y huérfanas.

\clubpenalty=9999
\widowpenalty=9999
\displaywidowpenalty=9999
\setlength{\emergencystretch}{3em}

% ── Numeración romana ─────────────────────────────────────────────────────────

% Versalitas por defecto
\makeatletter
$if(romansmall)$
% Los romanos se quedan en minúsculas
$else$
\renewcommand{\@roman}[1]{\capsfont{\metacaps{\romannumeral#1}}}
$endif$
\makeatother

% ── Listas (enumitem) ─────────────────────────────────────────────────────────

\usepackage{enumitem}

\setlist{
  topsep     = 0pt,
  partopsep  = 0pt,
  itemsep    = 0pt,
  parsep     = 0pt,
  labelwidth = \dimexpr\ParIndent - 0.5em\relax,
  labelsep   = 0.5em,
  leftmargin = !,
}

\AtBeginDocument{
  \setlist[itemize,1]{label=\textemdash}
  \setlist[itemize,2]{label=\textbullet}
  \setlist[itemize,3]{label={\small◆}}
  \setlist[itemize,4]{label=\textemdash}
$if(noindent)$
  \setlist{topsep=\parskip}
$endif$
}

\providecommand{\tightlist}{}
$if(noindent)$
\renewcommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}}
$endif$

% ── Citas exentas ────────────────────────────────────────────────────────────

\usepackage{quoting}

\quotingsetup{
  leftmargin  = \ParIndent,
  rightmargin = $if(quotesymmetric)$\ParIndent$else$0pt$endif$,
  vskip       = $if(noindent)$\parskip$else$0pt$endif$,
  indentfirst = false,
}

\AtBeginEnvironment{quoting}{%
  \textsmall
}

\renewenvironment{quote}{\begin{quoting}}{\end{quoting}}

% ── Idioma ───────────────────────────────────────────────────────────────────

\usepackage{babel}
\babelprovide[main, import, hyphenrules=spanish]{spanish}
\babelprovide[import]{french}
\babelprovide[import]{british}
\usepackage{selnolig}

% ── Tablas y flotantes ────────────────────────────────────────────────────────

\usepackage{booktabs}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{graphicx}
\usepackage{caption}

\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother

\usepackage{footnotehyper}
\makesavenoteenv{longtable}

\setlength{\textfloatsep}{0.5\baselineskip plus 4pt minus 2pt}
\setlength{\intextsep}{0.5\baselineskip plus 4pt minus 2pt}

\DeclareCaptionFont{captionfont}{\capsfont}
\DeclareCaptionLabelFormat{displaylabel}{\metacaps{#1}~#2}
\DeclareCaptionLabelSeparator{enspace}{\enspace}

\captionsetup{
  labelfont       = captionfont,
  labelsep        = enspace,
  labelformat     = displaylabel,
  singlelinecheck = false,
  justification   = justified,
}
\renewcommand{\captionfont}{\textsmall}
\captionsetup[figure]{position = below}
\captionsetup[table]{position = above, skip = 4pt}

\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{%
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi% select the smaller of both
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi%
}
\def\fps@figure{htbp}
\makeatother

% ── Notas al pie ─────────────────────────────────────────────────────────────

% Filete separador: solo en páginas de continuación por defecto.
$if(nofootrule)$
\usepackage[norule]{footmisc}
$elseif(footrule)$
\usepackage[splitrule]{footmisc}
$else$
\usepackage[norule,splitrule]{footmisc}
$endif$

\setlength{\skip\footins}{0.5\baselineskip plus 2pt minus 2pt}

$if(colorlinks)$
\renewcommand{\thefootnote}{\textcolor{black}{\arabic{footnote}}}
$else$
\renewcommand{\thefootnote}{\arabic{footnote}}
$endif$

% Formato del texto de la nota.
\makeatletter
\renewcommand{\@makefntext}[1]{%
  \textsmall
  \parindent=\ParIndent
  \@enumdepth=0\relax
  \@itemdepth=0\relax
  \noindent
$if(noindent)$%
% Usamos el comportamiento por defecto: notas al pie sin sangría
$else$%
  \hspace*{\ParIndent}%
$endif$%
$if(footsuper)$%
  {\@makefnmark}\enspace%
$else$%
  {\@thefnmark.}\enspace%
$endif$%
  #1%
}
\makeatother

% ── Encabezados y pie de página (fancyhdr) ───────────────────────────────────

\usepackage{fancyhdr}

$if(headscaps)$
  \newcommand{\headerstyle}[1]{\textsmall\metafont\metacaps{#1}}
$else$
  \newcommand{\headerstyle}[1]{\textsmall\metafont\textit{#1}}
$endif$

\renewcommand{\chaptermark}[1]{\markboth{#1}{}}

\pagestyle{fancy}
\fancyhf{}

\fancyhead[RO,LE]{\textsmall\metafont\thepage}

$if(folioonly)$
% Dejamos el encabezado vacío
$else$
  \fancyhead[RE]{\headerstyle{$if(author)$$for(author)$$author$$sep$, $endfor$$else$$title$$endif$}}
  \fancyhead[LO]{\nouppercase{\headerstyle{\leftmark}}}
$endif$

\renewcommand{\headrulewidth}{$if(headrule)$0.4pt$else$0pt$endif$}

\fancypagestyle{plain}{%
  \fancyhf{}%
  $if(nofolio)$
  % Dejamos el pie vacío
  $else$
    \fancyfoot[C]{\textsmall\metafont\thepage}%
  $endif$
  \renewcommand{\headrulewidth}{0pt}%
}

% Los capítulos sin numerar también cuentan
\makeatletter
\pretocmd{\@schapter}{\chaptermark{#1}}{}{}
\makeatother

% ── Secciones y títulos (titlesec) ────────────────────────────────────────────

\usepackage{needspace}
\usepackage[compact]{titlesec}

% Páginas en blanco intercaladas (twoside): sin encabezado ni folio.
\makeatletter
\renewcommand{\cleardoublepage}{%
  \clearpage
  \if@twoside
    \ifodd\c@page\else
      \hbox{}\thispagestyle{empty}\newpage
    \fi
  \fi
}
\makeatother

% ── Capítulo ──────────────────────────────────────────────────────────────────

\titleformat{\chapter}[display]
  {\metafont\fontsize{16}{20}\selectfont}
  {}
  {0pt}
  {}
\titlespacing*{\chapter}
  {0pt}{0pt}{2\baselineskip}

% ── Sección y subsecciones ────────────────────────────────────────────────────

\titleformat{\section}
  {\metafont\fontsize{14}{18}\selectfont}
  {\thesection}{1em}{}

\titleformat{\subsection}
  {\metafont\normalsize\bfseries}
  {\thesubsection}{1em}{}

\titleformat{\subsubsection}
  {\metafont\normalsize\bfseries\itshape}
  {\thesubsubsection}{1em}{}

\titleformat{\paragraph}[hang]
  {\metafont\normalsize\itshape}
  {\theparagraph}{1em}{}

\titleformat{\subparagraph}[hang]
  {\metafont\normalsize}
  {\thesubparagraph}{1em}{}

% ── Formato de los índices ───────────────────────────────────────────────────

\makeatletter

\newlength{\tocsubindent}
\setlength{\tocsubindent}{0.5\ParIndent}

\newcommand{\toc@line}[4]{%
  \begingroup
    \metafont
    \leftskip=#1\relax
    \hangindent=\tocsubindent
    \parindent=\z@
    \leavevmode
    #2{#3}\if\relax\detokenize{#4}\relax\else\nobreak{\upshape\enspace\textperiodcentered\enspace#4}\fi%
    \par
  \endgroup
}

% Capítulo: versalitas.
\renewcommand*{\l@chapter}[2]{%
  \ifnum\c@tocdepth>\m@ne
    \addpenalty{-\@highpenalty}%
    \addvspace{0.2\baselineskip}%
    \begingroup
      \def\numberline##1{}%
      \toc@line{0pt}{\MakeUppercase}{#1}{}%
    \endgroup
  \fi}

% Sección: redonda.
\renewcommand*{\l@section}[2]{%
  \ifnum\c@tocdepth>\z@
    \begingroup
      \def\numberline##1{}%
      \toc@line{\tocsubindent}{\upshape}{#1}{#2}%
    \endgroup
  \fi}

% Subsección: cursiva.
\renewcommand*{\l@subsection}[2]{%
  \ifnum\c@tocdepth>\@ne
    \begingroup
      \def\numberline##1{}%
      \toc@line{2\tocsubindent}{\itshape}{#1}{#2}%
    \endgroup
  \fi}

% Subsubsección: cursiva, más sangrada.
\renewcommand*{\l@subsubsection}[2]{%
  \ifnum\c@tocdepth>\tw@
    \begingroup
      \def\numberline##1{}%
      \toc@line{3\tocsubindent}{\itshape}{#1}{#2}%
    \endgroup
  \fi}

% Lista de figuras: redonda, sin sangrado.
\renewcommand*{\l@figure}[2]{%
  \begingroup
    \def\numberline##1{##1\enspace}%
    \toc@line{0pt}{\upshape}{#1}{#2}%
  \endgroup
}

% Lista de tablas: redonda, sin sangrado.
\renewcommand*{\l@table}[2]{%
  \begingroup
    \def\numberline##1{##1\enspace}%
    \toc@line{0pt}{\upshape}{#1}{#2}%
  \endgroup
}

\makeatother

% ── Código fuente (fancyvrb + tokens de resaltado) ────────────────────────────

\usepackage{fancyvrb}
\usepackage{framed}

$highlighting-macros$

% ── Bibliografía CSL (citeproc/pandoc) ────────────────────────────────────────

\NewDocumentCommand\citeproctext{}{}
\NewDocumentCommand\citeproc{mm}{%
  \begingroup\def\citeproctext{#2}\cite{#1}\endgroup}
\makeatletter
  \let\@cite@ofmt\@firstofone
  \def\@biblabel#1{}
  \def\@cite#1#2{{#1\if@tempswa , #2\fi}}
\makeatother
\newlength{\cslhangindent}
\setlength{\cslhangindent}{\ParIndent}
\newlength{\csllabelwidth}
\setlength{\csllabelwidth}{3em}
\newenvironment{CSLReferences}[2]
  {\begin{list}{}{%
    \setlength{\itemindent}{0pt}
    \setlength{\leftmargin}{0pt}
    \setlength{\parsep}{0pt}
    \ifodd #1
      \setlength{\leftmargin}{\cslhangindent}
      \setlength{\itemindent}{-1\cslhangindent}
    \fi
    \setlength{\itemsep}{#2\parskip}}}
  {\end{list}}
\usepackage{calc}
\newcommand{\CSLBlock}[1]{\hfill\break\parbox[t]{\linewidth}{\strut\ignorespaces#1\strut}}
\newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{\strut#1\strut}}
\newcommand{\CSLRightInline}[1]{\parbox[t]{\linewidth - \csllabelwidth}{\strut#1\strut}}
\newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}

% ── Cabeceras extra de Pandoc y del usuario ───────────────────────────────────

$for(header-includes)$
$header-includes$
$endfor$

% ── Hipervínculos y medatados ─────────────────────────────────────────────────

\usepackage{xurl}
\urlstyle{$if(urlstyle)$$urlstyle$$else$same$endif$}

$if(links-as-notes)$
\DeclareRobustCommand{\href}[2]{#2\footnote{\url{#1}}}
$endif$
$if(verbatim-in-note)$
\VerbatimFootnotes
$endif$

\usepackage{hyperref}

\providecommand{\xmpquote}[1]{#1}

\hypersetup{
$if(title-meta)$
  pdftitle={$title-meta$$if(subtitle)$. $subtitle$$endif$},
$endif$
$if(author-meta)$
  pdfauthor={$author-meta$},
$endif$
$if(lang)$
  pdflang={$lang$},
$endif$
$if(subject)$
  pdfsubject={$subject$},
$endif$
$if(keywords)$
  pdfkeywords={$for(keywords)$\xmpquote{$keywords$}$sep$, $endfor$},
$endif$
$if(colorlinks)$
  colorlinks=true,
  linkcolor={$if(linkcolor)$$linkcolor$$else$purpleblue$endif$},
  urlcolor={$if(urlcolor)$$urlcolor$$else$purpleblue$endif$},
  citecolor={$if(citecolor)$$citecolor$$else$purpleblue$endif$},
  filecolor={$if(filecolor)$$filecolor$$else$black$endif$},
$else$
$if(boxlinks)$
$else$
  hidelinks,
$endif$
$endif$
  pdfcreator={LaTeX via pandoc}
}

\usepackage{etoolbox}
\makeatletter
\pretocmd{\addcontentsline}{\phantomsection}{}{}
\makeatother

% ── Tráiler PDF de Pandoc ─────────────────────────────────────────────────────

$if(pdf-trailer-id)$
\pdfvariable trailerid {[ $pdf-trailer-id$ ]}
$endif$

% ── Portada ───────────────────────────────────────────────────────────────────

$if(title)$
\title{$title$$if(subtitle)$\\\medskip\large $subtitle$$endif$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
\date{$date$}

% ──────────────────────────────────────────────────────────────────────────────
\begin{document}
% ──────────────────────────────────────────────────────────────────────────────

\frontmatter

% ── Portada ──────────────────────────────────────────────────────────────────

$if(maketitle)$
\makeatletter
\patchcmd{\@maketitle}{\newpage}{}{}{}
\makeatother
\pdfbookmark[1]{Portada}{portada}
\maketitle
\thispagestyle{empty}
$endif$

% ── Resumen ───────────────────────────────────────────────────────────────────

$if(abstract)$
\clearpage
\thispagestyle{empty}
\newenvironment{abstract}{%
  \normalsize\noindent\textbf{Resumen}\par\noindent\ignorespaces
}{\par}
\begin{abstract}
$abstract$
$if(keywords)$
\par\noindent\textbf{Palabras clave:} $for(keywords)$$keywords$$sep$, $endfor$
$endif$
\end{abstract}
$endif$

% ── Dedicatoria ───────────────────────────────────────────────────────────────

$if(dedication)$
\clearpage
\thispagestyle{empty}
\vspace*{0.25\textheight}
\noindent\hfill
\begin{minipage}{0.5\linewidth}
  \setstretch{1}%
  \textit{$dedication$}
\end{minipage}
$endif$

% ── Epígrafe ──────────────────────────────────────────────────────────────────

$if(epigraphtext)$
\clearpage
\thispagestyle{empty}
\vspace*{0.25\textheight}
\noindent\hfill
\begin{minipage}{0.5\linewidth}
  \setstretch{1}%
  $epigraphtext$
  $if(epigraphauthor)$
  \par\smallskip
  {\raggedleft%
  \capsfont{$epigraphauthor$}%
  $if(epigraphwork)$, \textit{$epigraphwork$}$endif$\par}
  $else$$if(epigraphwork)$
  \par\smallskip{\raggedleft\textit{$epigraphwork$}\par}
  $endif$$endif$
\end{minipage}
$endif$

% ── Índices ───────────────────────────────────────────────────────────────────

{%
$if(colorlinks)$
\hypersetup{linkcolor=$if(toccolor)$$toccolor$$else$black$endif$}
$endif$

$if(toc)$
\clearpage
\thispagestyle{plain}
\pdfbookmark[1]{\contentsname}{toc}
\setcounter{tocdepth}{$if(toc-depth)$$toc-depth$$else$2$endif$}
\tableofcontents
$endif$

$if(lof)$
\clearpage
\thispagestyle{plain}
\addcontentsline{toc}{chapter}{\listfigurename}
\listoffigures
$endif$

$if(lot)$
\clearpage
\thispagestyle{plain}
\addcontentsline{toc}{chapter}{\listtablename}
\listoftables
$endif$
}

% ── Agradecimientos ───────────────────────────────────────────────────────────

$if(thanks)$
\clearpage
\chapter*{Agradecimientos}
$thanks$
$endif$

% ── Otros elementos preliminares ──────────────────────────────────────────────

$for(include-before)$
$include-before$
$endfor$

% ── Cuerpo principal ──────────────────────────────────────────────────────────

\mainmatter

$body$

% ── Elementos finales ─────────────────────────────────────────────────────────

\backmatter

$if(nocite-ids)$
\nocite{$for(nocite-ids)$$it$$sep$, $endfor$}
$endif$

$for(include-after)$
$include-after$
$endfor$

\end{document}
